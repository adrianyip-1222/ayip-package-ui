namespace AYip.UI.Pages
{
    /// <summary>
    /// It is used for stacking up in the PageManager.
    /// It could be either pages, page models or any other stackable items.
    /// </summary>
    public interface IStackable { }
}